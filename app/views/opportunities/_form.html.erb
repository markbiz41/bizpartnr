<%= simple_form_for(@opportunity, as: 'opportunity')  do |f| %>
  <% if @opportunity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@opportunity.errors.count, "error") %> prohibited this opportunity from being saved:</h2>

      <ul>
      <% @opportunity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <%= f.input :expertise_desired %>
    <%= f.input :partnership_opportunity %>
    <%= f.input :compensation, as: :integer, label: 'Compensation (Please put in only numbers for compensation without commas.)' %>

    <div class="row">
        <div class="col-xs-3">
        <%= f.association :industry, collection:  Industry.all.order('lower(name) ASC'), input_html: {class: 'col-xs-2'} %>
      </div>
    </div>

<br/>
<br/>
    <%= f.submit class: 'btn btn-primary' %>
<% end %>
